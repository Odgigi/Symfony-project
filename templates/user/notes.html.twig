{% extends 'base.html.twig' %}

{% block title %}User-Notes{% endblock %}

{% block body %}
    <h1>Mes notes</h1>
TODO: mettre en place template recoupant toutes les notes de ce user par annonce_id (et non note_id)
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Note_insolite</th>
                <th>Note_pratique</th>
                <th>Note_style</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for note in notes %}
            <tr>
                <td>{{ note.id }}</td>
                <td>{{ note.noteInsolite }}</td>
                <td>{{ note.notePratique }}</td>
                <td>{{ note.noteStyle }}</td>
                <td>
                    <a href="{{ path('note_show', {'id': note.id}) }}">show</a>
                    <a href="{{ path('note_edit', {'id': note.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('note_new') }}">Create new</a> <!--Supprimer: create_new Note à partir de show Annonce-->
{% endblock %}
