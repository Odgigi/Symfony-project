{% extends 'base.html.twig' %}

{% block title %}Note{% endblock %}

{% block body %}
    <h1>Note</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ note.id }}</td>
            </tr>
            <tr>
                <th>Note_insolite</th>
                <td>{{ note.noteInsolite }}</td>
            </tr>
            <tr>
                <th>Note_pratique</th>
                <td>{{ note.notePratique }}</td>
            </tr>
            <tr>
                <th>Note_style</th>
                <td>{{ note.noteStyle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('note_index') }}">back to list</a>

    <a href="{{ path('note_edit', {'id': note.id}) }}">edit</a>

    {{ include('note/_delete_form.html.twig') }}
{% endblock %}
